package BasicCode;

import javax.swing.plaf.OptionPaneUI;

import org.graalvm.compiler.core.common.type.ArithmeticOpTable.BinaryOp.Add;

public class Calc {

    public int calculator()
    {
        //Add
        if opps is Add{ return x + y}
        //Sub
        if opps is sub return x - y
        // Mult
        //Div
    }
    
    public int addition(int x, int y)
    {return x + y;}
    public int subtraction(int x, int y)
    {return x - y;}

    public calcMethod(int x, int y , opps)
    {
        if opps == add (addition(x, y))
    }   
    
    def name(varialbe)[do stuuff]
    public static int 
}
